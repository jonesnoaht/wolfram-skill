# Use nix-direnv for fast flake-based development shells

# Basic shell (no Wolfram)
use flake

# If you have manually added the Wolfram Engine installer, use this instead:
# use flake .#with-wolfram

# Optional: load project-specific environment variables here
# dotenv
